{
  "name": "{{product-name}}",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "format": "eslint . --fix",
    "lint": "eslint .",
    "test": "jest",
    "viewExamples": "vite -c vite.viewExamples.config.ts --port 3001",
    "generateIcons": "node --experimental-specifier-resolution=node --loader ts-node/esm ./scripts/generateIcons.ts"
  }
}
